import Link from "next/link"; import { ArrowLeft } from "lucide-react"; import { Button } from "@/components/ui/button"; import { createServiceTypeAction } from "@/features/services/actions"; import { prisma } from "@/lib/db/prisma";
export default async function ServiceTypesPage(){const types=await prisma.serviceType.findMany({include:{_count:{select:{services:true}}},orderBy:[{sortOrder:"asc"},{name:"asc"}]});return <><Link href="/owner/services" className="inline-flex items-center gap-2 text-sm font-semibold text-secondary"><ArrowLeft className="size-4"/>Kembali ke layanan</Link><h1 className="mt-8 font-display text-3xl font-extrabold">Jenis layanan</h1><form action={createServiceTypeAction} className="mt-6 grid gap-3 rounded-[16px] border border-border p-5 md:grid-cols-4"><input name="name" required placeholder="Nama, mis. Mobile Development" className="h-11 rounded-[10px] border border-border px-3"/><input name="slug" required placeholder="mobile-development" className="h-11 rounded-[10px] border border-border px-3"/><input name="icon" defaultValue="globe" className="h-11 rounded-[10px] border border-border px-3"/><Button>Buat jenis</Button></form><div className="mt-6 space-y-3">{types.map(type=><div key={type.id} className="flex justify-between rounded-[12px] border border-border p-4"><span className="font-semibold">{type.name}</span><span className="text-sm text-secondary">{type._count.services} layanan · {type.isActive?"Aktif":"Nonaktif"}</span></div>)}</div></>}
