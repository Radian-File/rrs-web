import Link from "next/link";
import { ArrowRight, Clock3, Search } from "lucide-react";
import { SiteFooter } from "@/components/layout/site-footer";
import { SiteHeader } from "@/components/layout/site-header";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { prisma } from "@/lib/db/prisma";
import { formatIdr } from "@/lib/utils";
import { getLocale } from "@/i18n/server";
import { PageEntrance } from "@/components/page-entrance";
import { getDictionary } from "@/i18n/dictionaries";
import { ServiceTypeFilter } from "@/components/service-type-filter";

export const dynamic = "force-dynamic";

export default async function ServicesPage({ searchParams }: { searchParams: Promise<{ q?: string; type?: string }> }) {
  const [{ q: rawQuery, type: rawType }, locale, types] = await Promise.all([searchParams, getLocale(), prisma.serviceType.findMany({ where: { isActive: true }, include: { _count: { select: { services: { where: { isPublished: true } } } } }, orderBy: [{ sortOrder: "asc" }, { name: "asc" }] })]);
  const dictionary = getDictionary(locale); const isId = locale === "id"; const q = rawQuery?.trim() ?? "";
  const type = rawType && types.some((item) => item.slug === rawType) ? rawType : undefined;
  const services = await prisma.service.findMany({ where: { isPublished: true, ...(type ? { serviceType: { slug: type, isActive: true } } : {}), ...(q ? { OR: [{ title: { contains: q, mode: "insensitive" } }, { summary: { contains: q, mode: "insensitive" } }, { technologies: { has: q } }] } : {}) }, orderBy: [{ isFeatured: "desc" }, { title: "asc" }] });
  const activeLabel = type ? `${types.find((item)=>item.slug===type)?.name}${q ? ` · “${q}”` : ""}` : q ? `“${q}”` : null;
  return <><SiteHeader/><PageEntrance><main><section className="border-b border-border"><div className="mx-auto max-w-[1280px] px-5 py-20 md:px-8 lg:px-16"><Badge>{isId?"Layanan terpilih":"Curated services"}</Badge><h1 className="text-balance mt-5 max-w-4xl font-display text-5xl font-extrabold tracking-[-0.05em] md:text-6xl">{isId?"Keahlian untuk project digital dengan proses yang jelas.":"Expertise for digital projects that need a clear process."}</h1><p className="mt-6 max-w-2xl text-lg leading-8 text-secondary">{isId?"Jelajahi kemampuan dan estimasi harga awal. Scope, timeline, dan harga final selalu disusun dalam quotation setelah diskusi.":"Browse capabilities and starting estimates. Every final scope, timeline, and price is prepared as a quotation after discussion."}</p><form className="mt-9 flex max-w-2xl flex-col gap-3 sm:flex-row"><div className="relative flex-1"><Search className="absolute left-4 top-1/2 size-5 -translate-y-1/2 text-secondary"/><Input name="q" defaultValue={q} placeholder={isId?"Cari web development, design, atau teknologi":"Search web development, design, or technology"} className="h-14 pl-12"/></div><ServiceTypeFilter types={types.map((item)=>({slug:item.slug,name:item.name,count:item._count.services}))} value={type} query={q} label={isId?"Jenis layanan":"Service type"} allLabel={isId?"Semua layanan":"All services"}/><Button size="sm" className="h-14 sm:self-auto">{isId?"Cari":"Search"}</Button></form></div></section><section data-reveal className="mx-auto max-w-[1280px] px-5 py-20 md:px-8 lg:px-16"><div className="mb-7 flex flex-wrap items-center justify-between gap-3"><p className="text-sm text-secondary"><span className="font-semibold text-foreground">{services.length}</span> {dictionary.common.results}{activeLabel&&` · ${activeLabel}`}</p>{(q||type)&&<Button asChild variant="ghost" size="sm"><Link href="/services">{dictionary.common.clearFilters}</Link></Button>}</div><div data-reveal-group className="grid gap-5 lg:grid-cols-3">{services.map((service)=><Card key={service.id} data-reveal-item data-motion-card className="group overflow-hidden"><CardContent className="flex h-full flex-col"><div data-card-media className="h-44 rounded-[10px] bg-[linear-gradient(145deg,#e8f0ec,#d6e4dc)]"/><p className="mt-6 text-xs font-bold uppercase tracking-[.13em] text-primary">{service.category}</p><h2 className="mt-3 font-display text-xl font-extrabold">{service.title}</h2><p className="mt-3 text-sm leading-6 text-secondary">{service.summary}</p><div className="mt-5 flex flex-wrap gap-2">{service.technologies.slice(0,3).map((tag)=><Badge key={tag} variant="neutral">{tag}</Badge>)}</div><div className="mt-auto flex items-end justify-between border-t border-border pt-6"><div><p className="text-xs text-secondary">{isId?"Mulai dari":"Starting from"}</p><p className="mt-1 font-display text-lg font-extrabold">{service.startingPrice?formatIdr(service.startingPrice.toString()):(isId?"Scope khusus":"Custom scope")}</p></div><span className="flex items-center gap-1 text-xs text-secondary"><Clock3 className="size-4"/>{service.deliveryEstimate}</span></div><Button asChild variant="outline" className="mt-6 w-full"><Link href={`/services/${service.slug}`}>{isId?"Lihat layanan":"View Service"}<ArrowRight className="size-4"/></Link></Button></CardContent></Card>)}</div>{services.length===0&&<div className="rounded-[20px] border border-dashed border-border p-12 text-center"><h2 className="font-display text-xl font-extrabold">{isId?"Layanan tidak ditemukan":"No matching service"}</h2><p className="mt-2 text-sm text-secondary">{isId?"Coba pencarian yang lebih umum atau hapus filter.":"Try a broader search or clear the filters."}</p><Button asChild variant="outline" className="mt-5"><Link href="/services">{dictionary.common.clearFilters}</Link></Button></div>}</section></main></PageEntrance><SiteFooter/></>;
}
