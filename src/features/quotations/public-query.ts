import { prisma } from "@/lib/db/prisma";
import { hashPublicToken } from "@/lib/security/tokens";

export async function getPublicQuotation(token:string){const hash=hashPublicToken(token);const quote=await prisma.quotation.findUnique({where:{publicTokenHash:hash},include:{items:{orderBy:{sequence:"asc"}},paymentTerms:{orderBy:{sequence:"asc"}},revisionRequests:{orderBy:{createdAt:"desc"}}}});if(!quote||quote.publicTokenRevokedAt||!quote.isCurrent)return null;if(quote.validUntil<new Date()&&["SENT","VIEWED"].includes(quote.status)){return prisma.quotation.update({where:{id:quote.id},data:{status:"EXPIRED",activities:{create:{type:"EXPIRED",description:"Quotation validity period ended."}}},include:{items:{orderBy:{sequence:"asc"}},paymentTerms:{orderBy:{sequence:"asc"}},revisionRequests:{orderBy:{createdAt:"desc"}}}});}if(["SENT","VIEWED"].includes(quote.status)){return prisma.quotation.update({where:{id:quote.id},data:{status:"VIEWED",viewCount:{increment:1},viewedAt:quote.viewedAt??new Date(),lastViewedAt:new Date(),activities:quote.status==="SENT"?{create:{type:"VIEWED",description:"Client opened the secure quotation link."}}:undefined},include:{items:{orderBy:{sequence:"asc"}},paymentTerms:{orderBy:{sequence:"asc"}},revisionRequests:{orderBy:{createdAt:"desc"}}}});}return quote;}
